# WinCtrl X-Plane Plugin

The X-Plane SDK is documented in 'xplane-sdk-toolkit'. If this directory is missing the user must run `git clone git@github.com:jamesmorrison/xplane-sdk-toolkit.git` to install.

AI is not allowed to run ANY git commands without exlicitly checking. 'git add' and 'git commit' are always forbidden and can NEVER be run by AI.